Essai Git pour GISMEAUX

Information : pour ne pas avoir à retaper le mot de passe à chaque 'push', il suffit de créer un fichier .netrc à la racine de l'utilisateur (/home/user ou /root selon le cas) et de mettre dedans les lignes suivantes.


machine <servername>
login <username>
password <password>


Avec <servername> étant github.com et <username>/<password> vos identifiants Github.

(Note: ceci ne marche pas chez moi, si c'est le cas une autre solution est de rajouter le mot de passe dans l'URL du dépôt, donc https://user:password@github.com/...)
